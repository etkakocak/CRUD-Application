<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Snippet</title>
    <link rel="stylesheet" href="/css/style.css"> 
    <link rel="stylesheet" href="/css/create-snippet.css"> 
</head>
<body>
    <nav>
        <div class="logo">Code Snippets</div>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/snippets">View Snippets</a></li>
            <li><a href="/auth/logout">Logout</a></li>
        </ul>
    </nav>

    <section class="snippet-form-container">
        <h1>Edit Snippet</h1>

        <% if (error_msg) { %>
            <p class="error-message"><%= error_msg %></p>
        <% } %>

        <form action="/snippets/edit/<%= snippet._id %>" method="POST">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= snippet.title %>" required>

            <label for="language">Programming Language:</label>
            <input type="text" id="language" name="language" value="<%= snippet.language %>" required>

            <label for="code">Code:</label>
            <textarea id="code" name="code" rows="6" required><%= snippet.code %></textarea>

            <button type="submit">Save Changes</button>
        </form>

        <a href="/snippets" class="cancel-link">Cancel</a>
    </section>
</body>
</html>
